# MobileNetV4-Conv-Small + BiLSTM
experiment:
  name: "mobilenetv4_conv_small_bilstm"

model:
  backbone: "mobilenetv4_conv_small"
  temporal_head: "bilstm"
  hidden_dim: 128
  n_layers: 2
  dropout: 0.3

training:
  epochs: 50
  lr: 1.0e-3
  weight_decay: 1.0e-4
  early_stopping_patience: 10
