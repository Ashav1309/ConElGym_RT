# MobileNetV3-Small + TSM + BiLSTM
experiment:
  name: "mobilenet_v3_small_tsm_bilstm"

model:
  backbone: "mobilenet_v3_small_tsm"
  temporal_head: "bilstm"
  hidden_dim: 128
  n_layers: 2
  dropout: 0.3

training:
  epochs: 50
  lr: 1.0e-3
  weight_decay: 1.0e-4
  early_stopping_patience: 10
