# MobileNetV3-Small + CausalTCN temporal head
experiment:
  name: "mobilenet_v3_tcn"

model:
  backbone: "mobilenet_v3_small"
  temporal_head: "causal_tcn"
  dropout: 0.2

training:
  epochs: 50
  lr: 1.0e-3
  weight_decay: 1.0e-4
  early_stopping_patience: 10
